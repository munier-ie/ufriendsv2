import 'package:flutter/material.dart';

/// Placeholder — Gov & Identity Services screen.
/// NIN Slip and BVN Slip have been moved to their own dedicated screens
/// accessible directly from the Services tab.
/// This screen is reserved for future content.
class GovServicesScreen extends StatelessWidget {
  const GovServicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Gov Services — Coming Soon')),
    );
  }
}
